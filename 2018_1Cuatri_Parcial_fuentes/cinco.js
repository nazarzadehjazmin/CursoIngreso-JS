/*
Bienvenidos (SWITCH).
pedir el ingreso de un planeta del sistema solar
Si es la tierra mostrar "acá vivimos".
Si está más cerca del sol, "acá hace más calor".
Si está más lejos del sol, "acá hace más frio".
Si no es un planeta valido informarlo.
Aclaración: los planetas se ingresan en
minúscula (Mercurio y Venus están antes que la tierra). */

/*
//SWITCH
function mostrar() {
    let planeta;

    planeta = prompt("Ingrese el nombre de un planeta del sistema solar");

    switch (planeta) {
        case "mercurio":
        case "venus":
            alert("Acá hace más calor");
            break;
        case "tierra":
            alert("Acá vivimos");
            break;
        case "marte":
        case "júpiter":
        case "saturno":
        case "urano":
        case "neptuno":
            alert("Acá hace más frío");
            break;
        default:
            alert("No es un planeta válido");
            break;
    }

}
*/




//IF

/*
function mostrar() {
    let planeta;

    planeta = prompt("Ingrese un planeta del sistema solar");

    if (planeta == "tierra") {
        alert("Acá vivimos");
    } else {
        if (planeta == "marte" || planeta == "júpiter" || planeta == "saturno" || planeta == "urano" || planeta == "neptuno") {
            alert("Acá hace más frío");
        } else {
            if (planeta == "mercurio" || planeta == "venus") {
                alert("ACá hace más calor");
            } else {
                alert("No es un planeta válido");
            }
        }
    }
}

*/




//IF Y SWITCH
