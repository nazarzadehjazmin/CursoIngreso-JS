function mostrar() {
	let edad;

	edad = txtIdEdad.value;
	edad = parseInt(edad);
	//edad = parseInt(txtIdEdad.value);

	if (edad == 15) {
		alert("niña bonita");
	}

}//FIN DE LA FUNCIÓN


//IF toma todos los valores como verdaderos a excepción del 0 y false, 
//esto es porque siempre evalúa la condición como true

/*
!!!
anido ifs si tengo que escribir varias veces la misma condicion
si tengo que escribir una sola vez la condicion, esta bien usar operadores logicos
*/
